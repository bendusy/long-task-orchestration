# Samples

Samples are frozen evidence from historical LTO runs. They are kept for
reproducibility and promotion review, not as current command documentation.

Some pre-v0.5.0 samples may cite paths from the retired Python fallback. Treat
those paths as historical evidence only; the supported CLI path is Rust-owned
`lto-rs`, with old-run compatibility preserved by fixtures and release notes.
