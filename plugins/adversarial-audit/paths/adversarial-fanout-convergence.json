{
  "id": "adversarial-fanout-convergence-v1",
  "intent": "audit",
  "description": "Fan-out heterogeneous refute-first audit squad, union-merge findings into blocker register, iterate until HIGH+CRITICAL count converges.",
  "steps": [
    {
      "step": 1,
      "label": "fanout-audit",
      "primitive": "runner",
      "parallelizable": true,
      "profiles": [
        "codex-refuter-v1",
        "pi-refuter-v1",
        "agy-refuter-v1",
        "claude-refuter-v1"
      ],
      "note": "Dispatch the profiles in parallel against the same artifact. Each runner must be a different model family from the artifact producer; when the host agent is claude, exclude claude-refuter-v1 (same-family self-audit). Read-only caveat: agy cannot enforce read-only, so the scheduler fail-closes and rejects agy dispatch for read-only audit (2026-06-10 verified); do not dispatch read-only audit tasks to agy/gemini.",
      "expected_artifact": "findings-round-N.json"
    },
    {
      "step": 2,
      "label": "union-merge",
      "primitive": "audit",
      "action": "union-merge findings from all runners into blocker-register-round-N.json",
      "note": "Do NOT deduplicate or vote. Every finding from every runner enters the register. Identical claims may appear multiple times; that is intentional signal.",
      "expected_artifact": "blocker-register-round-N.json"
    },
    {
      "step": 3,
      "label": "host-triage",
      "primitive": "judge",
      "action": "host reads each blocker, verifies cited path:line evidence, classifies adopt/reject/needs-info",
      "note": "LLM claim alone is not evidence. Host must open and read the cited file location. Rejected blockers require a falsification note. Divergence routing: when findings contradict each other on facts, the host adjudicates by reading the cited sources first-hand; when the disagreement is about direction (no evidence can adjudicate), do not force a verdict here — hand off to a direction-review flow (prose pointer only, no hard inter-plugin dependency).",
      "expected_artifact": "triage-round-N.json"
    },
    {
      "step": 4,
      "label": "convergence-check",
      "primitive": "next",
      "check": "HIGH+CRITICAL count in triage <= previous round AND zero new HIGH/CRITICAL blockers in this round",
      "on_converged": "proceed to closeout",
      "on_not_converged": "apply adopted fixes, increment N, return to step 1",
      "note": "Two consecutive clean rounds required before declaring convergence. If count is non-monotonic (rebounds), pause and re-examine audit scope or artifact."
    }
  ],
  "stop_conditions": [
    "HIGH+CRITICAL blocker count monotonically non-increasing across all rounds",
    "two consecutive rounds with zero new HIGH/CRITICAL blockers",
    "every adopted/rejected blocker has first-hand evidence (path:line or command output)",
    "no permission escalation beyond read-only",
    "divergence exit: contradictory findings are adjudicated by host first-hand reads; direction-level disagreement with no adjudicating evidence is routed to a direction-review flow instead of being settled inside this path"
  ],
  "anti_patterns": [
    "ap1:same-family self-audit — auditor and producer share model family",
    "ap2:vote-to-merge-findings — majority vote used instead of union merge",
    "ap3:historical-contrast hallucination — before/after context shown to auditor",
    "ap4:rubber-stamp PASS — empty findings accepted without proof-of-read",
    "ap5:claude-host-claude-refuter — host agent is claude but claude-refuter-v1 is dispatched anyway (same-family self-audit; exclude it when the host is claude)"
  ]
}
