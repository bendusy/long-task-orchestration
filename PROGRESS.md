# Progress

- Baseline: `cargo test --locked --all-targets` passed, 472 tests.
- Baseline worktree: clean (`git status --short` produced no paths).
- Scope: backend enum dispatch refactor only; no herdr implementation.
